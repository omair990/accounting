<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Payment Register Wizard: Form View -->
    <record id="view_account_payment_register_form" model="ir.ui.view">
        <field name="name">account.payment.register.form</field>
        <field name="model">account.payment.register</field>
        <field name="arch" type="xml">
            <form string="Register Payment">
                <group>
                    <group>
                        <field name="partner_id" readonly="1"/>
                        <field name="amount_residual" readonly="1"/>
                        <field name="amount"/>
                        <field name="currency_id" groups="base.group_multi_currency"/>
                    </group>
                    <group>
                        <field name="journal_id"/>
                        <field name="payment_date"/>
                        <field name="memo"/>
                        <field name="payment_type" readonly="1"/>
                    </group>
                </group>
                <field name="invoice_ids" invisible="1"/>
                <footer>
                    <button name="action_create_payment" string="Create Payment"
                        type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action to open wizard -->
    <record id="action_payment_register_wizard" model="ir.actions.act_window">
        <field name="name">Register Payment</field>
        <field name="res_model">account.payment.register</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>
